import React, { Component } from 'react';
import { SignupStyle as styles } from 'styles';

export default class Signup extends Component {
  	render() {
		return (
	  		<div id={styles.signupid}>
                Signup page
            </div>
		);
  	}
}
